### 执行同步代码​​

同步代码直接推入调用栈执行（如 console.log、变量赋值）。
​​
### 处理微任务队列​​

同步代码执行完毕后，立即清空微任务队列中的所有任务。​​若微任务中产生新的微任务，会继续执行直到队列为空​​（例如递归调用 Promise.then）。
​​
### 执行一个宏任务​​

从宏任务队列中取出一个任务执行（如 setTimeout 回调）。
​​
### 更新渲染（浏览器环境）​​

如果需要，执行 UI 渲染操作（如重绘、回流）。
​​
### 循环重复​​

重复上述步骤，形成持续的事件循环。
