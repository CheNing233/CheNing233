## 起因

在翻别人的库的示例的时候，偶然看见这种写法

```ts
import TokenizeWorker from './Tokenize?worker';
```

看着挺正常的，一细看，我测怎么有个问号

难不成这个文件也是什么可选择语法？

后来看准几遍，前面是个完整的文件名，后面是个`?worker`

突然理解了这很可能是一个`Web Worker`，但是我从来没见过这么引入的

后来一查，这种方式在`Vite`中是有效的